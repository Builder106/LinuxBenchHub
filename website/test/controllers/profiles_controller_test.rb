require "test_helper"

# Scaffold leftover: profiles/edit and profiles/update sit behind Devise auth so
# they redirect for unauthenticated requests. Replace with real coverage when
# sign-in helpers are added.
class ProfilesControllerTest < ActionDispatch::IntegrationTest
end
